<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Detail Pariwisata</title>
    <link rel="stylesheet" href="https://unpkg.com/leaflet@1.7.1/dist/leaflet.css" />
    <link href="https://fonts.googleapis.com/css2?family=Montserrat:wght@400;500;600;700&display=swap" rel="stylesheet">
    <style>
        body {
            font-family: 'Montserrat', sans-serif;
            background-color: #aaddd8;
            color: #333;
            margin: 0;
            padding: 0;
        }

        h1 {
            font-size: 36px;
            text-align: center;
            margin: 20px 0;
        }

        .container {
            max-width: 900px;
            margin: 60px auto;
            padding: 20px;
            background-color: #c3e4e0;
            border-radius: 8px;
            box-shadow: 0 0 10px rgba(0, 0, 0, 0.1);
            box-sizing: border-box;
        }

        .pariwisata-info {
            text-align: left;
            margin-bottom: 20px;
            margin: 20px;
            padding: 20px;
            border-radius: 8px;
            background-color: #E3FEF7;
            box-shadow: 0 0 8px rgba(0, 0, 0, 0.1);
        }

        .pariwisata-info h2 {
            margin: 10px 0;
            font-size: 28px;
            font-weight: 600;
            text-align: center;
            color: #003C43;
        }

        .pariwisata-info img {
            width: 100%;
            border-radius: 8px;
            margin-top: 20px;
            max-height: 450px;
        }

        .pariwisata-info p {
            font-size: 16px;
            line-height: 1.6;
            margin-top: 10px;
            text-align: justify;
            color: #003C43;
        }

        .pariwisata-info strong {
            font-weight: 600;
        }

        .pariwisata-info .kabupaten {
            text-align: center;
            margin-top: 10px;
            font-size: 18px;
            color: #003C43;
        }

        .reviews-container {
            display: flex;
            flex-wrap: wrap;
            justify-content: space-between;
            gap: 20px;
            margin-top: 20px;
        }

        .reviews {
            flex: 1 1 48%;
            padding: 15px;
            background-color: #E3FEF7;
            color: #003C43;
            border-radius: 8px;
            box-shadow: 0 0 8px rgba(0, 0, 0, 0.1);
            box-sizing: border-box;
        }

        .reviews h3 {
            margin-bottom: 10px;
            padding-bottom: 8px;
            border-bottom: 1px solid #ccc;
            font-size: 20px;
            font-weight: 600;
        }

        .reviews ul {
            list-style: none;
            padding: 0;
            margin: 10px 0;
        }

        .reviews li {
            margin-bottom: 10px;
            padding: 10px;
            border: 1px solid #ccc;
            border-radius: 4px;
            font-size: 16px;
        }

        .add-review {
            flex: 1 1 48%;
            padding: 15px;
            background-color: #E3FEF7;
            border-radius: 8px;
            box-shadow: 0 0 8px rgba(0, 0, 0, 0.1);
            box-sizing: border-box;
            color: #003C43;
        }

        .add-review h3 {
            margin-bottom: 10px;
            font-size: 20px;
            font-weight: 600;
        }

        .add-review form {
            margin-top: 10px;
        }

        .add-review form label {
            display: block;
            margin-bottom: 5px;
            font-size: 16px;
        }

        .add-review form input[type="text"],
        .add-review form textarea,
        .add-review form input[type="number"],
        .add-review form button {
            width: 100%;
            padding: 10px;
            margin-top: 10px;
            border: 1px solid #ccc;
            border-radius: 4px;
            box-sizing: border-box;
            font-size: 16px;
        }

        .add-review form button {
            background-color: #28a745; 
            width: fit-content;
            color: #fff;
            border: none;
            cursor: pointer;
            font-weight: 600;
            transition: background-color 0.3s ease;
        }

        .add-review form button:hover {
            background-color: #218838;
        }

        .btn-back {
            display: inline-block;
            background-color: #6c757d;
            color: #fff;
            border: none;
            padding: 10px 20px;
            border-radius: 4px;
            cursor: pointer;
            text-decoration: none;
            font-weight: 600;
            margin-bottom: 10px;
            text-align: center;
            width: fit-content;
            transition: background-color 0.3s ease;
        }

        .btn-back:hover {
            background-color: #5a6268;
        }

        @media (max-width: 830px) {
            h1 {
                font-size: 28px;
            }

            .container {
                padding: 15px;
                margin: 50px 20px;
            }

            .pariwisata-info h2 {
                font-size: 24px;
            }

            .pariwisata-info p,
            .pariwisata-info .kabupaten,
            .reviews h3,
            .reviews li,
            .add-review h3,
            .add-review form label,
            .add-review form input[type="text"],
            .add-review form textarea,
            .add-review form input[type="number"],
            .add-review form button {
                font-size: 12px;
            }

            .reviews-container {
                flex-direction: column;
                gap: 15px;
            }

            .reviews, .add-review {
                flex: 1 1 100%;
                min-width: auto;
            }

            i {
                font-size: 12px;
            }

            .btn-back{
                font-size: 12px;
            }
        }

        @media (max-width: 480px) {
            h1 {
                font-size: 18px;
            }

            .pariwisata-info h2 {
                font-size: 16px;
            }

            .pariwisata-info p,
            .pariwisata-info .kabupaten,
            .reviews h3,
            .reviews li,
            .add-review h3,
            .add-review form label,
            .add-review form input[type="text"],
            .add-review form textarea,
            .add-review form input[type="number"],
            .add-review form button {
                font-size: 12px;
            }
        }
    </style>
</head>
<body>
    <div class="container">
        <a class="btn-back"  href="/"> <i class="fas fa-arrow-left"></i> Kembali</a>

        <div class="pariwisata-info">
            <h2><%= pariwisata.judul %></h2>
            <div class="kabupaten"><strong>Kabupaten:</strong> <%= pariwisata.kabupaten %></div>
            <img src="/uploads/<%= pariwisata.gambar %>" alt="<%= pariwisata.judul %>">
            <p><%= pariwisata.deskripsi %></p>
            <p><strong>Fasilitas:</strong> <%= pariwisata.fasilitas %></p>
            <p><strong>Harga Tiket:</strong> <%= pariwisata.hargaTiket %></p>
            <p><strong>Lokasi:</strong> <%= pariwisata.lokasi %></p>
        </div>

        <p style="color: #003C43; text-align: center; font-size: 14px;"> <i>Klik lokasi dibawah untuk menuju ke gmaps</i></p>
        <div id="map" style="height: 400px; border-radius: 8px; box-shadow: 0 0 8px rgba(0, 0, 0, 0.1); margin-top: 20px;"></div>
        <div class="reviews-container">
            <div class="reviews">
                <h3>Ulasan</h3>
                <% if (reviews.length > 0) { %>
                    <ul>
                        <% reviews.forEach(review => { %>
                            <li><strong><%= review.user %>:</strong> <%= review.komentar %> (Rating: <%= review.rating %>)</li>
                        <% }); %>
                    </ul>
                <% } else { %>
                    <p>Belum ada ulasan untuk pariwisata ini.</p>
                <% } %>
            </div>

            <div class="add-review">
                <h3>Tambah Ulasan</h3>
                <form method="post" action="/api/pariwisata/<%= pariwisata.id %>/review">
                    <div>
                        <label for="user">Nama:</label>
                        <input type="text" id="user" name="user" required>
                    </div>
                    <div>
                        <label for="komentar">Komentar:</label>
                        <textarea id="komentar" name="komentar" rows="3" required></textarea>
                    </div>
                    <div>
                        <label for="rating">Rating:</label>
                        <input type="number" id="rating" name="rating" min="1" max="5" required>
                    </div>
                    <button type="submit" href="reviews-container">Kirim Ulasan</button>
                </form>
            </div>
        </div>
    </div>

    <script src="https://unpkg.com/leaflet@1.7.1/dist/leaflet.js"></script>
    <script>
        document.addEventListener('DOMContentLoaded', function () {
            var latitude = `<%= pariwisata.latitude %>`;
            var longitude = `<%= pariwisata.longitude %>`;

            var map = L.map('map').setView([latitude, longitude], 13);

            L.tileLayer('https://{s}.tile.openstreetmap.org/{z}/{x}/{y}.png', {
                attribution: '&copy; <a href="https://www.openstreetmap.org/copyright">OpenStreetMap</a> contributors'
            }).addTo(map);

            var marker = L.marker([latitude, longitude]).addTo(map)
                .bindPopup('Lokasi Pariwisata.<br> Latitude: ' + latitude + '<br>Longitude: ' + longitude)
                .openPopup();

            function openInGoogleMaps(lat, lng) {
                var url = `https://www.google.com/maps?q=${lat},${lng}`;
                window.open(url, '_blank');
            }

            marker.on('click', function() {
                openInGoogleMaps(latitude, longitude);
            });
        });
        
    </script>
</body>
</html>
