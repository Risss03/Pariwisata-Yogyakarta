<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Edit Pariwisata</title>
    <link href="https://fonts.googleapis.com/css2?family=Montserrat:wght@400;500;600;700&family=Poppins:wght@400;500;600;700&display=swap" rel="stylesheet">
    <style>
        body {
            display: flex;
            justify-content: center;
            align-items: center;
            min-height: 100vh;
            margin: 0;
            background-color: #E3FEF7;
            font-family: 'Poppins', sans-serif;
            color: #333;
            padding: 10px;
            box-sizing: border-box;
        }

        .form-container {
            width: 80%;
            margin: 30px;
            padding: 10px;
            background-color: #fff;
            border: 1px solid #ccc;
            border-radius: 8px;
            box-shadow: 0 0 10px rgba(0, 0, 0, 0.1);
            box-sizing: border-box;
        }

        .form-group {
            margin-bottom: 20px;
        }

        .form-group label {
            display: block;
            margin-bottom: 5px;
            font-size: 18px;
            font-weight: 600;
        }

        .form-control, .form-control-file {
            width: 100%;
            padding: 12px;
            margin-top: 5px;
            border: 1px solid #ccc;
            border-radius: 4px;
            box-sizing: border-box;
            font-size: 16px;
        }

        .form-control::placeholder, .form-control-file::placeholder {
            color: #999;
            font-size: 14px;
        }

        .form-control:focus, .form-control-file:focus {
            border-color: #007bff;
            outline: none;
        }

        .btn {
            display: inline-block;
            padding: 12px 24px;
            color: #fff;
            border: none;
            border-radius: 4px;
            cursor: pointer;
            text-align: center;
            text-decoration: none;
            font-size: 16px;
            font-weight: 600;
            width: fit-content;
            transition: background-color 0.3s ease;
        }

        .btn-primary {
            background-color: #e9b311;
        }

        .btn-primary:hover {
            background-color: #dfac15;
        }

        .btn-secondary {
            background-color: #6c757d;
            margin-right: 10px;
        }

        .btn-secondary:hover {
            background-color: #5a6268;
        }

        .button-group {
            display: flex;
            justify-content: flex-end;
            margin-top: 20px;
        }

        @media (max-width: 768px) {
            .form-container {
                padding: 20px;
            }

            .form-group label {
                font-size: 14px;
            }

            h1{
                font-size: 24px;
            }

            .form-control, .form-control-file {
                padding: 10px;
                font-size: 12px;
            }

            .btn {
                padding: 10px 20px;
                font-size: 12px;
            }
        }

        @media (max-width: 480px) {
            .form-container {
                padding: 15px;
            }

            .form-group label {
                font-size: 14px;
            }

            .form-control, .form-control-file {
                padding: 8px;
            }

            .btn {
                padding: 8px 16px;
                font-size: 12px;
            }

            .button-group {
                align-items: stretch;
            }

            .btn-secondary {
                margin-bottom: 10px;
                height: 15px;
            }
            
            .btn-primary {
            height: 30px;
        }

            h1 {
                font-size: 18px;
            }

            .form-control, .form-control-file{
                font-size: 12px;
            }
        }
    </style>
</head>
<body>
    <div class="form-container">
        <h1>Edit Pariwisata</h1>
        <form action="/admin/pariwisata/<%= pariwisata.id %>/update" method="post" enctype="multipart/form-data">
            <div class="form-group">
                <label for="judul">Judul</label>
                <input type="text" id="judul" name="judul" class="form-control" value="<%= pariwisata.judul %>" required>
            </div>
            <div class="form-group">
                <label for="kabupaten">Kabupaten</label>
                <input type="text" id="kabupaten" name="kabupaten" class="form-control" value="<%= pariwisata.kabupaten %>" required>
            </div>
            <div class="form-group">
                <label for="deskripsi">Deskripsi</label>
                <textarea id="deskripsi" name="deskripsi" class="form-control" rows="4" required><%= pariwisata.deskripsi %></textarea>
            </div>
            <div class="form-group">
                <label for="fasilitas">Fasilitas</label>
                <textarea id="fasilitas" name="fasilitas" class="form-control" rows="4" required><%= pariwisata.fasilitas %></textarea>
            </div>
            <div class="form-group">
                <label for="hargaTiket">Harga Tiket</label>
                <input type="text" id="hargaTiket" name="hargaTiket" class="form-control" value="<%= pariwisata.hargaTiket %>" required>
            </div>
            <div class="form-group">
                <label for="lokasi">Lokasi</label>
                <textarea id="lokasi" name="lokasi" class="form-control" rows="4" required><%= pariwisata.lokasi %></textarea>
            </div>
            <div class="form-group">
                <label for="gambar">Gambar</label>
                <input type="file" id="gambar" name="gambar" class="form-control-file">
                <input type="hidden" name="existingGambar" value="<%= pariwisata.gambar %>">
                <img src="/uploads/<%= pariwisata.gambar %>" alt="<%= pariwisata.judul %>" style="max-width: 100px; margin-top: 10px; display: block;">
            </div>
            <div class="form-group">
                <label for="latitude">Latitude</label>
                <input type="text" id="latitude" name="latitude" class="form-control" value="<%= pariwisata.latitude %>" placeholder="Latitude" required>
            </div>
            <div class="form-group">
                <label for="longitude">Longitude</label>
                <input type="text" id="longitude" name="longitude" class="form-control" value="<%= pariwisata.longitude %>" placeholder="Longitude" required>
            </div>
            <div class="button-group">
                <a href="/admin/dashboard" class="btn btn-secondary">Cancel</a>
                <button type="submit" class="btn btn-primary">Update</button>
            </div>
        </form>
    </div>
</body>
</html>
